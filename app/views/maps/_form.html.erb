<%= form_with(model: map, local: true) do |form| %>
  <% if map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
        <% map.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :clients_id %>
    <%= form.text_field :clients_id %>
  </div>

  <div class="field">
    <%= form.label :usstates_id %>
    <%= form.text_field :usstates_id %>
  </div>

  <div class="field">
    <%= form.label :ceus_id %>
    <%= form.text_field :ceus_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
